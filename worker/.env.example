# Your worker token (Settings -> Workers in the Squared One dashboard)
WORKER_TOKEN=

# Base URL of the Squared One server (only needed if it is not the default)
WORKER_URL=https://squared-one.onrender.com
